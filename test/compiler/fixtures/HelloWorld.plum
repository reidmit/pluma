module HelloWorld
export (b, fn)

b = 47

list = [1, 2, 3, 4]

fn = a => b => c => "hello, world!"

conditional = x => if x then True else False

greet = name => "hi ${name}!"

withLetExpression = firstName =>
  let
    lastName = "test"
  in
    "hi ${firstName} ${lastName}!"

fn 1 2 3

obj = { a: 1, b
}

p1 = s => "nice"
p2 = s => "ok"
p3 = a => b => "test"
piped = "reid" |> p1 |> p3 "hi" |> greet
piped2 = p1 "hello" |> greet
