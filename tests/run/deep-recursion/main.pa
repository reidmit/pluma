# Tail-call-style recursion through the bytecode VM. Frames live on the
# heap, so this is bounded only by memory rather than the Rust call stack.

def count-down fun n {
  when n is 0 {
    ()
  } is _ {
    count-down (n - 1)
  }
}

def main fun {
  count-down 5000
  print "ok"
}
