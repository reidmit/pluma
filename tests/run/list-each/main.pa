use core.list

def main fun {
  list.each [1, 2, 3] fun n {
    print (to-string (n * 10))
  }

  # each returns nothing — `print` it to confirm
  let result = list.each ["a", "b"] fun s { print s }
  print (to-string result)
}
