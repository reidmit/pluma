use core.dict

def main = fun {
	let m = dict.empty ()
	dict.insert m "alice" 30
	dict.insert m "bob" 25
	dict.insert m "carol" 28

	# keys/values/entries iterate in insertion order.
	print (dict.keys m)
	print (dict.values m)
	print (dict.entries m)
}
