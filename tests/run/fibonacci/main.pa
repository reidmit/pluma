def fib fun n {
  when n is 0 {
    0
  } is 1 {
    1
  } is _ {
    (fib (n - 1)) + (fib (n - 2))
  }
}

def main fun {
  print (to-string (fib 0))
  print (to-string (fib 1))
  print (to-string (fib 10))
}
