use core.dict

def main = fun {
	# bytes as dict keys — exercises hash bytes
	let m = dict.empty ()
	let m = dict.insert m 'GET' 1
	let m = dict.insert m 'POST' 2
	let m = dict.insert m 'PUT' 3
	when (dict.lookup m 'POST') is some v { print v } is none { print -1 }
	when (dict.lookup m 'GET') is some v { print v } is none { print -1 }
	when (dict.lookup m 'DELETE') is some v { print v } is none { print -1 }

	# ord bytes — exercise compare
	print (compare 'abc' 'abd')
	print (compare 'abc' 'abc')
	print (compare 'abd' 'abc')
	print (compare '' 'a')
}
