use core.regex

def hello /"hello"/

def main fun {
  print (to-string (regex.matches hello "hello, world!"))
  print (to-string (regex.matches hello "goodbye, world!"))
  print (to-string (regex.matches hello "hello"))
  print (to-string (regex.matches hello ""))
}
