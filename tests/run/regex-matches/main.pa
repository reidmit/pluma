use core.regex

def hello = /"hello"/

def main = fun {
	print (regex.matches hello "hello, world!")
	print (regex.matches hello "goodbye, world!")
	print (regex.matches hello "hello")
	print (regex.matches hello "")
}
