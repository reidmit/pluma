use colors
use math

def name-of = fun c {
	when c is colors.color.red {
		"red"
	} is colors.color.green {
		"green"
	} is colors.color.blue {
		"blue"
	}
}

def main = fun {
	print (math.add 2 3)
	print (name-of colors.color.green)
}
