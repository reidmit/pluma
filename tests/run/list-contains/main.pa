use core.list

def main fun {
  print (to-string (list.contains [1, 2, 3] 2))
  print (to-string (list.contains [1, 2, 3] 99))
  print (to-string (list.contains [] 1))
  print (to-string (list.contains ["a", "b", "c"] "b"))
  print (to-string (list.contains [(1, 2), (3, 4)] (3, 4)))
}
