use core.list

def main fun {
  let xs = [1, 2, 3, 4, 5]
  print (to-string (list.map xs fun n { n * n }))
  print (to-string (list.filter xs fun n { (n % 2) == 0 }))
  print (to-string (list.map (list.filter xs fun n { n > 2 }) fun n { n + 100 }))
}
