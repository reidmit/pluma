# `option.expect` on `none` aborts with an `option.expect:`-labelled message --
# an option has no error payload to show beyond it. `after` never prints.
use std/list

def empty :: list int = []

def main = fun {
	print "before"
	print (list.head empty |> option.expect "the list was empty")
	print "after"
}
