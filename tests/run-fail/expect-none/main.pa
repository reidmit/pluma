# `option.expect` on `none` aborts with just the message -- an option has
# no error payload to show. `after` never prints.
use std.list

def empty :: list int = []

def main = fun {
	print "before"
	print (list.head empty |> option.expect "the list was empty")
	print "after"
}
