# `crash` is the always-in-scope way to stop the program with a message: it
# writes the message to stderr, exits nonzero, and prints a backtrace pointing
# at the crash site. `after` never prints.
def main = fun {
	print "before"
	crash "stopping on purpose"
	print "after"
}
