use std/assert
use std/task
use std/test
use std/sys/io

def do-nothing = fun {}

def run = fun {
	do-nothing ()
	print "ok"
}

def tests :: test.suite = [
	test.case "empty-fun-body" fun {
		try cap = io.capture fun {
			run ()
			task.return ()
		}
		assert.matches cap.out "ok\n"
	},
]
