use std.task
use std.request

# A function argument can't cross the wire, so this endpoint is rejected at
# the boundary with attribution naming the offending parameter.
public remote def apply-twice :: fun request (fun int -> int) int -> task int = fun _req f n {
	task.return (f (f n))
}
