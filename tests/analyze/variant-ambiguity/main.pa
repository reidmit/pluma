enum color {
	red
	green
}

enum fruit {
	red
	apple
}

# `red` is ambiguous when the subject's enum type isn't known
# (here `c` is a fresh type var until the pattern resolves it).
def check = fun c {
	if c is red {
		0
	}
}
