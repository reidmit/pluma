enum color {
	red
	green
}

enum fruit {
	red
	apple
}

# Bare `red` must be qualified: two enums declare it and the subject's type
# isn't known here (`c` is a fresh type var), so the error names both
# `color.red` and `fruit.red`.
def check = fun c {
	if c is red {
		0
	}
}
