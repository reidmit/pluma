# Mixing `try` carriers in one chain is a type error: the first `try` pins
# the chain's carrier (here `result`), so a following `task` `try` can't
# unify (`task _` vs `result _`).
def bad = fun {
	try x = ok 1
	try y = task.return 2
	task.return (x + y)
}
