enum tree {
	empty
	node int tree tree
}

def missing-variant = fun t {
	when t is tree.empty {
		0
	}
}

def partial-constructor = fun t {
	when t is tree.empty {
		0
	} is tree.node 0 _l _r {
		1
	}
}

def missing-bool-case = fun b {
	when b is true {
		0
	}
}
