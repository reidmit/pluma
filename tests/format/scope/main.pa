use std/task
def demo = fun {
	scope as s {
		let h = s.spawn work
		try a = h
		task.return a
	}
}

def race-ish = fun ts {
	manual scope as s {
		try first = s.next ()
		s.cancel ()
		task.return first
	}
}

def anon = fun {
	scope {
		task.return 1
	}
}
