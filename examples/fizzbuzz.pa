fizzbuzz = fun n -> if (n % 3, n % 5)
  is (0, 0) then "FizzBuzz"
  is (0, _) then "Fizz"
  is (_, 0) then if env.get "LIGHTYEAR"
    is some val then val
    is none then "Buzz"
  is _ then n | stringify

main = fun ->
  1 .. 100 | each fun i ->
    fizzbuzz i | print