def fizzbuzz = fun n {
  when (n % 3, n % 5) is (0, 0) {
    "FizzBuzz"
  } is (0, _) {
    "Fizz"
  } is (_, 0) {
    "Buzz"
  } is _ {
    stringify n
  }
}

def main = fun {
  for i in 1..100 {
    print (fizzbuzz i)
  }
}