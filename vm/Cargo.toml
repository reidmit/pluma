[package]
name = "vm"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
compiler = { path = "../compiler" }
rand = "0.10"
regex = "1"
serde_json = { version = "1", features = ["preserve_order"] }
uuid = { version = "1", features = ["v4", "v7"] }
jiff = "0.2.25"
