[package]
name = "vm"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
compiler = { path = "../compiler" }
rand = "0.10"
uuid = { version = "1", features = ["v4", "v7"] }
jiff = "0.2.25"
