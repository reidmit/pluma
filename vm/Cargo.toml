[package]
name = "vm"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
compiler = { path = "../compiler" }
regex = "1"
serde_json = { version = "1", features = ["preserve_order"] }
