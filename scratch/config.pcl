use core/pkg/*

name = "reid"
version = "1.2.0"

test-files = concat [
  files-matching `any ".test." any`
]

dependencies = [
  pkg/git (
    repo: "https://github.com/reidmit/something"
    branch: "main"
  )
  git (
    repo: "https://github.com/reidmit/something"
    branch: "main"
  )
]