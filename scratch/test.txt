# This is a function that takes in two integers
# and adds them together, returning their result.
add-nums a b : int int -> int =
  print "adding ${a} and ${b}!";
  a + b


repeat-times 10 \ i ->
  print "current is ${i}!"



data = [
  1
  2
  3
  4
]


data | each \ i ->
  result = add-nums i 10;
  print "result: ${result}"


main args : list<string> -> done =
  options = cli/parse-arguments args;
  print "got options: ${options}";

  sum = box 0;

  data | each \ i ->
    sum.value = sum.value + 1;

  print "sum is: ${sum.value}"


main args =
  p = box (person ("reid", 28));
  years = 47;

  repeat-times 47 \
    p.value = person (...p.value, age: p.value.age + 1);

  done

ref = struct<a> (
  value: mutable<a>
)

main args =
  p = ref (person ("reid", 28));
  years = 47;

  print "initial age is: ${p.age}"

  repeat-times 47 \
    p.value.age.value <- p.value.age.value + 1;

  print "age is now: ${p.age}"

  done


counter = struct (
  current: ref<int>
)


repeat-times 47 \
  counter.current <- @counter.current + 1


main _ =
  r = ref 0;
  r <- 1;
  print "r is now: ${@r}"


while cond func
  : (() -> bool) (() -> done) -> done
  = loop \ call cond
    ? true -> call func; std/loop/continue
    ? false -> std/loop/break

i = ref 0;
while (\ i.value < 10) \
  i.value <- i.value + 1

i
  ? ref (value: 1) -> print "it's one!"
  ? _ ->              print "oh ok"


my-ref = struct (value: mutable<int>)

person = struct (
  name: string
  age: mutable<int>
)

p = person ("reid", 28)

p.age <- p.age + 1


: <e> mutable<list<e>> e
push element list =
  # ... internal?


main args =
  v = mut-list [];
  v | push 1;
  v | push 2;
  print "length: ${v | len}"


phrases = nouns | zip phrases | each \ pair -> "${pair.0} ${pair.1}"



maybe-xx
  ? some(xx) -> print "ok"
  ? _ -> done;