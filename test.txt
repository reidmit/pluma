let s = "reid"
let z = 10

let add-nums : int int -> int = \ a b -> a + b

add-nums 1 2

let might-fail : result<string, string> =
  get-favorite-color
    ? colors/red -> ok "nice choice!"
    ? _ -> err "yikes"


point = (1, 2)

point
  ? (0, 0) -> print "zero zero"
  ? (0, 2) -> print "zero two"
  ? _      -> print "somewhere else: $(point.0) $(point.1)"


let arr = [1, 2, 3]

arr[1]


let arr = mut []

let x : maybe<int>
let x : maybe<int> = get-answer ()

let y : int = x ?? 0



